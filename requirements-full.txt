# Core
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0
requests>=2.31.0
httpx>=0.25.0
joblib>=1.3.0

# ML / Classical
scikit-learn>=1.3.0
xgboost>=2.0.0

# Deep Learning
tensorflow>=2.15.0

# Feature Store
# ---------------------------------------------------------------
# WINDOWS USERS: hopsworks depends on `twofish`, a C extension
# that requires compilation. If `pip install hopsworks` fails,
# use ONE of these fixes:
#
#   FIX 1 (Recommended — Conda):
#     conda install twofish
#     pip install hopsworks[python]
#
#   FIX 2 (pip only — install MS Build Tools first):
#     1. Download Visual C++ Build Tools:
#        https://visualstudio.microsoft.com/visual-cpp-build-tools/
#     2. Select "Desktop development with C++" during install
#     3. Restart terminal, then: pip install hopsworks
# ---------------------------------------------------------------
hopsworks>=3.7.0

# Explainability
shap>=0.44.0

# API
fastapi>=0.104.0
uvicorn>=0.24.0

# Dashboard
streamlit>=1.29.0
plotly>=5.18.0

# EDA (optional)
seaborn>=0.13.0
matplotlib>=3.8.0
statsmodels>=0.14.0

# Utilities
pytz>=2023.3
